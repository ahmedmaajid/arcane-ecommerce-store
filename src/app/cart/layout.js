export const metadata = {
  title: "Your Bag — Arcane",
  description:
    "Review your selected pieces and proceed to checkout. Complimentary shipping on all Arcane orders.",
};

export default function CartLayout({ children }) {
  return <>{children}</>;
}
